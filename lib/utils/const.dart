import 'package:cash_new_revenue/utils/colors.dart';
import 'package:flutter/material.dart';

const Icon navigNext = Icon(Icons.navigate_next, color: Color(0x56232C2D), size: 32,);
const Icon cancel = Icon(Icons.backspace_outlined, color: Color(0xFF000000));
const Icon search = Icon(Icons.search);
const Icon add = Icon(Icons.add);
const Icon back = Icon(Icons.navigate_before);

const Divider divider = Divider(
  height: 1,
  color: dividerColor,
  indent: 16,
);
